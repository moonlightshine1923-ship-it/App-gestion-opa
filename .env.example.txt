# ============================================================
#  OPA — Modèle de configuration (.env)
#  À copier en ".env" à la RACINE du projet (à côté de package.json)
#  Local :     cp .env.example .env   (puis adaptez si besoin)
#  cPanel :    créez le fichier .env via le Gestionnaire de fichiers
# ============================================================

# ---------- Base de données ----------
# EN LOCAL (XAMPP) :
#   DB_HOST=127.0.0.1
#   DB_PORT=3306
#   DB_USER=root
#   DB_PASSWORD=
#   DB_NAME=opa_db
#
# SUR CPANEL (exemple — remplacez par VOS vraies valeurs) :
#   DB_HOST=localhost
#   DB_PORT=3306
#   DB_USER=moncompte_opauser
#   DB_PASSWORD=MotDePasse_Réel_Ici
#   DB_NAME=moncompte_opa_db
# ⚠️ Sur cPanel, le nom de la base ET l'utilisateur ont TOUJOURS
#    un préfixe (= votre nom de compte cPanel + underscore).
#    Recopiez-les EXACTEMENT depuis « Bases de données MySQL »,
#    sans espace avant/après.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=opa_db

# ---------- Application ----------
PORT=3003
# Changez ce secret en production (invalide les anciens tokens) :
OPA_SECRET=changez-moi-en-production-xyz123

# ---------- Emails ----------
OPA_DIRECTION_EMAIL=direction@opa.dz
SMTP_USER=votre.compte@gmail.com
SMTP_PASS=xxxx xxxx xxxx xxxx
